#include <iostream>
#include <cmath>
#include <bits/stdc++.h>
using namespace std;
int main() { 
    int n;
    cin>>n;
   
      int aree[n];
    for (int i = 0; i < n; ++i){ cin >> aree[i];}
    sort(aree,aree+n);
    cout<<aree[n-1]-aree[0];
  
   
        
    
}
